import {IIData} from '../interfaces/dataInterface';

const useChangeName = (data: IIData, currentName: string, newName: string) => {
  console.log(currentName, newName);
};

export default useChangeName;
